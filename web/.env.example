# Base URL of the FastAPI service (app/api.py). Defaults to
# http://localhost:8000 if unset — see src/config.py's api_cors_origins,
# which already allows http://localhost:5173 (this app's Vite dev port).
VITE_API_BASE_URL=http://localhost:8000
